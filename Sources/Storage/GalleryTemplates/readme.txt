

This folder contains gallery template samples.

Strict rules:

1. All templates must be ZIP files
2. 	[filename].html.xslt  - name convention for html entry point file => as output we must get [filename].html
	[filename].css.xslt - name convension for css files => as output [filename].css